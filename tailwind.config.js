/** @type {import('tailwindcss').Config} */
module.exports = {
    content: [
        "./**/*.go.html",
        // add other paths if needed
    ],
    theme: {
        extend: {},
    },
    plugins: [],
}